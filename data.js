// data.js — stub only. All state managed by app.js via rb5_ prefixed localStorage keys.
// The old `state` object here caused the "adds but doesn't show" bug — two separate lists.
function saveState(){ /* intentional no-op */ }
